import * as React from "react";

// container -> view
// container -> connector -> view
//   Logic      share Logic  RN(or)R(the view is different for React and RN)
// controller -> connector -> view

export const RegisterConnector = () => (
  <div>
    <h1>RegisterConnector</h1>
  </div>
);
